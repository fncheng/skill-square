## 1. 扩展确认基础设施

- [x] 1.1 扩展 `ConfirmOptions` 和全局确认状态，增加可选 verification 配置，并确保确认关闭后清理该配置且不改变现有 `confirm()` Promise 行为。
- [x] 1.2 在 `useConfirm` 中增加强制传入 `expectedText` 的 `confirmDeletion()`，统一危险样式和“确认删除”按钮文本，同时保留普通 `confirm()`。
- [x] 1.3 实现确认文本规范化与匹配逻辑：Unicode NFC、移除首尾空白、区分大小写并保留内部空白和标点。
- [x] 1.4 更新全局 `ConfirmDialog`，展示期望文本和本地 `Input`，在匹配前禁用确认删除按钮，并在打开、目标变化、取消和确认后重置输入。
- [x] 1.5 补齐弹窗的 modal 语义、标题和输入标签关联、输入框自动聚焦、Enter 有效提交及 Escape 取消行为。

## 2. 接入资源删除入口

- [x] 2.1 将 Prompt 列表删除迁移到 `confirmDeletion()`，使用 Prompt 名称作为确认文本，并明确历史版本和标签关联会同时删除。
- [x] 2.2 将解决方案列表页和详情页删除迁移到 `confirmDeletion()`，使用解决方案标题作为确认文本，并明确全部批注会级联删除。
- [x] 2.3 将学习笔记列表页和详情页删除迁移到 `confirmDeletion()`，使用学习笔记标题作为确认文本，并明确全部批注会级联删除。
- [x] 2.4 将 UI 原型列表页和详情页删除迁移到 `confirmDeletion()`，使用 UI 原型标题作为确认文本并保留不可恢复提示。
- [x] 2.5 将分类和标签删除迁移到 `confirmDeletion()`，分别使用分类名称和标签名称作为确认文本，并保留未分类和关联移除影响提示。
- [x] 2.6 将批注的所有删除触发位置接入同一个 `confirmDeletion()` 流程，使用固定文本 `删除批注`，展示关联原文片段并明确 Markdown 原文不变。

## 3. 验证与回归

- [x] 3.1 静态搜索所有前端 DELETE API 调用、删除处理函数和删除按钮，确认所有用户可见删除入口均经过 `confirmDeletion()`，并确认 Prompt 回滚仍使用无输入框的普通 `confirm()`。
- [x] 3.2 验证匹配、错误输入、首尾空白、大小写差异、复制粘贴、Enter、Escape、取消后重开以及不同目标连续打开时的状态重置行为。
- [x] 3.3 在桌面端和窄视口检查长标题换行、输入框焦点、按钮 disabled 状态、弹窗滚动和辅助技术语义；如无法进行浏览器运行时验证，在交付中明确说明。
- [x] 3.4 按项目命令规则在取得用户许可后运行前端 `typecheck`；未获许可时完成静态类型审查并在交付中说明未执行编译类检查。
